header.myHeader(class="#{special}")
  nav.navbar.navbar-default.navbar-fixed-top
    //.lable typing is FUN
    .collapse.navbar-collapse
      ul.nav.navbar-nav
        li.active
          a.view_profile.btn(href='/') view profile
        li
          a.btn(href = '/typing') type now
        li
          a.btn(href = '/dbs') create article
        li
          a.btn(href = '/game') Gaming!
      ul.nav.navbar-nav.navbar-right
        li.user
          label#username #{user ? user.username : 'tourist'}
          - var url = user?'/images/logged.png':'/images/login.png'
          img#logState( src = url role="button" data-toggle="modal" data-target="#login-modal")
.holder
include form
